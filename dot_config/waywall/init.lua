return {
    actions = require("actions"),
    input = {
        layout = "us",
        remaps = require("remaps"),
        repeat_rate = 35,
        repeat_delay = 500,
    },
    theme = require("appearance")
}
