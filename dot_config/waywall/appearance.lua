local colors = require("colors")

return {
    background = colors.background or "#000000",
    ninb_anchor = "topleft", -- topleft, top, topright, left, right, bottomleft, bottomright
    ninb_opacity = 1.0
}
